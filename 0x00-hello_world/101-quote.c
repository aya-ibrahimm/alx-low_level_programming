#include <stdio.h>

/**
 * main - c program
 *
 *Return: return 0 (success)
 */
int main(void)
{
const char *message = "and that piece of art is useful\n";
write(2, message, 31);
return (1);
const char *message = "and that piece of art is useful\n";
    write(2, message, 31);
    return 1;
}}
